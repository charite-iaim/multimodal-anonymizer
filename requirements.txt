# Core dependencies
langchain>=0.1.0
langchain-openai>=0.0.5
langchain-fireworks>=0.1.0
langchain-core>=0.1.0
pydantic>=2.0.0

# Backend server
fastapi>=0.104.0
uvicorn>=0.24.0

# Environment configuration
python-dotenv>=1.0.0

# SSL certificate handling (for macOS compatibility)
certifi>=2023.0.0

# Image processing
Pillow>=10.0.0
pydicom>=2.4.0

# OCR for alternative processing mode
easyocr>=1.7.0

# TrOCR for handwritten text detection (uses custom fine-tuned model)
transformers>=4.36.0
safetensors>=0.4.0
torch>=2.0.0

# PDF processing
pdf2image>=1.16.0

# DOCX processing
python-docx>=1.1.0

# Excel processing
openpyxl>=3.1.0

# OpenAI-compatible API (for Fireworks)
openai>=1.0.0

# Audio processing (for voice anonymization - fully local)
openai-whisper>=20231117  # Local Whisper model for speech-to-text
kokoro-onnx>=0.4.0  # Kokoro TTS - lightweight ONNX-based text-to-speech (~300MB, Python 3.13 compatible)
soundfile>=0.12.0  # Audio file I/O for TTS
pydub>=0.25.0  # Audio format conversion (requires ffmpeg)

# Optional: For evaluation script
opencv-python>=4.8.0
numpy>=1.24.0
pandas>=2.0.0
matplotlib>=3.7.0

python-multipart>=0.0.6

pydub>=0.25.0
audioop-lts; python_version >= "3.13"